<h1>この人だ～れだ！</h1>
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>直接入力フォームの例</title>
</head>
<body>
    <form action="#" method="post">
        <label for="subject">その人の</label>
        <input type="text" id="subject" name="subject" placeholder="出身" required>
        <label for="predicate">は</label>
        <input type="text" id="predicate" name="predicate" placeholder="東京都" required>
        <label>ですか？</label>
        <br><br>
        <input type="submit" value="質問する">
    </form>
</body>
</html>

<table class="info-table">
  <thead>
    <tr>
      <th>氏名</th>
      <th>性別</th>
      <th>年齢</th>
      <th>出身</th>
      <th>職業</th>
    </tr>
  </thead>
  <tbody>
    <% @hints.each do |hint| %>
      <tr>
        <td><%= hint.name %></td>
        <td><%= hint.human_readable_gender %></td>
        <td><%= hint.age %></td>
        <td><%= hint.birthplace %></td>
        <td><%= hint.job %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<style>
  .info-table {
    width: 60%;
    border-collapse: collapse;
    margin-top: 20px;
  }
  .info-table th, .info-table td {
    border: 1px solid #ddd;
    padding: 8px 12px;
    text-align: left;
  }
  .info-table th {
    background-color: #f2f2f2;
    font-weight: bold;
  }
</style>
