<h1>記録一覧</h1>

<table class="score-table">
  <thead>
    <tr>
      <th>正解までの時間（秒）</th>
      <th>質問回数</th>
      <th>プレイ日時</th>
      <th>ギブアップ</th>
    </tr>
  </thead>
<tbody>
  <% @records.each do |record| %>
    <tr>
      <td><%= record.giveup ? "-" : record.cleartime %></td>
      <td><%= record.number_of_time %></td>
      <td><%= record.created_at.strftime("%Y/%m/%d %H:%M") %></td>
      <td style="text-align: center;">
        <input type="checkbox" <%= "checked" if record.giveup %> disabled>
      </td>
    </tr>
  <% end %>
</tbody>

</table>

<hr>
<%= link_to 'ゲーム画面へ戻る', game_path %>

<style>
.score-table {
  border-collapse: collapse;
  margin-top: 15px;
  width: 100%;
  max-width: 600px;
}

.score-table th:nth-child(1), /* 時間 */
.score-table td:nth-child(1) {
  width: 80px;
}

.score-table th:nth-child(2), /* 質問回数 */
.score-table td:nth-child(2) {
  width: 80px;
}

.score-table th:nth-child(4), /* ギブアップ */
.score-table td:nth-child(4) {
  width: 100px;
}

/* プレイ日時は自動で広がるように設定 */

.score-table th, .score-table td {
  border: 1px solid #ddd;
  padding: 8px;
  text-align: center;
}

.score-table th {
  background-color: #f2f2f2;
}

@media screen and (max-width: 768px) {
  .score-table {
    font-size: 14px;
  }
}
</style>
